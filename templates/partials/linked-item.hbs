<div class="item">
    <div class="linked-item" data-identifier="{{identifier}}">
        <input type="text" value="{{name}}"
            {{#if i18nKey }}placeholder="{{ localize i18nKey }}"{{/if}}
            readonly
        />
    </div>
</div>
