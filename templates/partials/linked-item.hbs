<div class="item" data-identifier="{{identifier}}">
    <div class="linked-item">
        <input type="text" value="{{name}}"
            placeholder="{{ localize i18nKey }}"
            readonly
        />
    </div>
</div>
